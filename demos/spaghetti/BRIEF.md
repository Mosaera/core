# Task

Add a **median** of the amounts to `report.py`, printed alongside the existing
`mean` (and a `north_median` alongside `north_mean`).

The script currently prints row count, total, mean, north_total and north_mean.
Keep it runnable as `python report.py <csv>`.
