# MCB-30: a stale COMMENT, nothing else. The first corpus case with no behavioural
# surface at all -- authored for #118, because 0 of the original 26 had this shape.
kind = "python"
capability = "non-behavioural"
tier = "trivial"
max_iterations = 4
budget_usd = 1.0
budget_tokens = 300000
budget_iterations = 4
